/**
 * Design philosophy reminder: preserve high-contrast light-mode tactical styling through Tailwind CSS 4 processing.
 */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
